{
  "name": "mcp-ssh-server",
  "version": "1.0.0",
  "description": "Model Context Protocol SSH Server - Bridge VS Code with remote Linux servers",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --inspect src/index.js",
    "test": "node --test tests/**/*.test.js",
    "test:watch": "node --test --watch tests/**/*.test.js"
  },
  "keywords": [
    "mcp",
    "ssh",
    "vscode",
    "copilot",
    "remote",
    "sftp"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "ssh2": "^1.15.0",
    "ssh2-sftp-client": "^10.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
